Carga de Datos
-- Preguntar si la carga a traves de CSV la ven correcta.
    -- Me dijo que si me hizo  algunas correcciones con el tema de la carga de reservas de  Redis,
     que cargar solo en redis aquellas que quedaron en el carrito, esas no deberian crearse en mongo porque son datos que quedan ahi en ram.
     2 opciones cargarlas aparte (una lista para redis y otra para mongo) u otra mas facil es agregar una que no tenga estado y esa cargarla en redis pero no en mongo 

Reservas
-- Dentro de MongoDB debemos guardar las reservas de estado "pendiente"? Me dijo que No, que pendiente en realidad es pendiente de pago, ahi solo van las que van en el carrito.
      que cargar solo en redis aquellas que quedaron en el carrito, esas no deberian crearse en mongo porque son datos que quedan ahi en ram.
     2 opciones cargarlas aparte (una lista para redis y otra para mongo) u otra mas facil es agregar una que no tenga estado y esa cargarla en redis pero no en mongo 

Cache de Busqueda
-- Que se prentende que se guarde? El cache de busqueda son busquedas de hoteles o destinos, aquellos datos que sean mas facil luego cuando el usuario quiera buscar
 de nuevo algo que ya busco, que no tenga que traerlo de nuevo desde la base. HISTORIAL POR USUARIO, HORA ,  HACER CON UN EXPIRE (BUSQUEDAS DE HOTELES O DESTINO)
-- En que punto de las consultas integradas deberíamos usarlo?

Relaciones FAMILIAR DE
-- En que punto de las consultadas integradas se debería usar? Veo que solo se hace referencia a la de AMIGO DE.
    -- Me dijo que si querias podias hacer una consulta adicional con usuarios, para probar, pero no es obligatoria

Consultas integradas
-- Dentro de la resolución de consultas esperan que este todo el codigo ahi, o se puede 
modularizar en funciones??
    
    - Si no hay problema con que modularicemos mientras que luego expliquemos todo en un documento de como pensamos todo (pdf adiconal)

Rutas
-- Hay manera de modificar las rutas?. Comentar que se incluyo las carpetas SRC y Fuentes dentro de la que se llama NOTEBOOK porque sino no se podia acceder desde jupyterLab. 
    --Dijo que no que jupyterLab funcion asi que te toma como raiz tu notebook. Pero que no pasa nada que eso si despues cuando lo pasemos todo este adentro del zip.

Tema entrega
-- Me dijo que lo podemos hacer en el mismo notebook explicando , en un readme o un pdf . Que es lo que mas importante del pdf
    -- Explicar que base utilizamos y para guardar que dato, como se penso, que datos se guardan en cada base, como planeamos la carga de datos  y que decisiones tomamos y porque .
    
Tema conectar el docker con vscode 
1) correr el docker primero en el jupyterLab (debe estar conectado ahi)
2) conectas el vscode al host del jupyterLab 

